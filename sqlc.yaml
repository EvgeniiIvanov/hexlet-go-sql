version: "2"
sql:
  - engine: sqlite
    schema: migrations
    queries:
      - query/users
      - query/courses
      - query/enrollments
      - query/orders
    gen:
      go:
        package: db
        out: internal/db
        emit_json_tags: true
        emit_interface: true
